# R
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# build artefacts
/*.tar.gz
/*.Rcheck/
..Rcheck/
/docs/
/doc/
/Meta/
inst/doc/

# OS
.DS_Store

# data (never commit fits or large data into the package)
*.rds
*.h5ad
data-raw/*.rds
# ...except the small shipped example dataset
!inst/extdata/*.rds
# ...and the frozen solver digests. Untracked, these made the bit-identity
# gate skip silently on every fresh clone and in CI -- the one test that
# proves the compiled core still reproduces the R it replaced.
!tests/testthat/*.rds

# compiled objects
src/*.o
src/core/*.o
src/*.so
src/*.dll
