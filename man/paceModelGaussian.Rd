% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/pace-core-gaussian.R
\name{paceModelGaussian}
\alias{paceModelGaussian}
\alias{paceModelGaussian,SpatialExperiment-method}
\title{Fit the PACE model to continuous intensities}
\usage{
paceModelGaussian(object, ...)

\S4method{paceModelGaussian}{SpatialExperiment}(
  object,
  celltype_col,
  image_col = NULL,
  condition_col = NULL,
  assay_name = "intensity",
  h_bio = 30,
  h_tech = 5,
  kernel_per_image = FALSE,
  image_re = c("none", "intercept", "slopes", "condition_slopes"),
  types = NULL,
  resp_term = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment}.}

\item{...}{Passed to \code{pace_fit_streaming_gaussian()}.}

\item{celltype_col}{Column of \code{colData} holding the cell type labels.}

\item{image_col}{Column of \code{colData} identifying the image. Defaults to the
single image when there is only one.}

\item{condition_col}{Optional column of \code{colData} giving a condition contrast.}

\item{assay_name}{The assay holding the intensities.}

\item{h_bio, h_tech}{Bandwidths of the biological and technical kernels.}

\item{kernel_per_image}{Build the kernels within each image separately.}

\item{image_re}{An optional second random-effect block over images.}

\item{types}{Cell types, in the order the fit should use them.}

\item{resp_term}{Optional response term passed to the model frame.}

\item{verbose}{Print the fitting trace.}
}
\value{
A \link{PACEFit}.
}
\description{
The Gaussian, identity-link form of \code{\link[=paceModel]{paceModel()}}, for continuous measurements
such as imaging mass cytometry protein intensities rather than counts.
}
\details{
The working response on the identity link is the intensity itself and the
working weight is the per-gene residual variance, so the mean converges in a
single inner solve and the outer iteration only moves the variance components.

This path does not model technical contamination. Correct the intensities
first, against an ambient field built with \code{\link[=ambientField]{ambientField()}}, and pass the
corrected matrix here.
}
