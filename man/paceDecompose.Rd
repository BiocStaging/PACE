% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/paceFit.R
\name{paceDecompose}
\alias{paceDecompose}
\alias{paceDecompose,PACEFit-method}
\title{Variance decomposition}
\usage{
paceDecompose(object, ...)

\S4method{paceDecompose}{PACEFit}(object, spe, ...)
}
\arguments{
\item{object}{A \link{PACEFit} from \code{\link[=paceModel]{paceModel()}}.}

\item{...}{Unused.}

\item{spe}{The \link[SpatialExperiment:SpatialExperiment]{SpatialExperiment::SpatialExperiment} that was fitted.}
}
\value{
The \code{PACEFit} with the variance decomposition added.
}
\description{
Partitions per-gene expression variance into cell-type identity, spatial cell
state, contamination, and residual (plus a responder block for condition
cohorts), populating \code{\link[=varianceDecomposition]{varianceDecomposition()}}. Needs the fitted object plus
the same \code{SpatialExperiment} used for \code{\link[=paceModel]{paceModel()}} (to read the counts).
}
\details{
The decomposition needs per-cell-type statistics of the fitted means, which
the fit stores; it never builds a \verb{cells x genes} matrix. An older fit without
those statistics has them computed from its stored fitted means, or, if those
were dropped, rebuilt exactly from the fit and \code{spe} a block of genes at a time.
}
\examples{
spe <- readRDS(system.file("extdata", "bc_xenium_subset.rds", package = "PACE"))
fit <- readRDS(system.file("extdata", "pace_fit_example.rds", package = "PACE"))
fit <- paceDecompose(fit, spe)
head(varianceDecomposition(fit))
}
